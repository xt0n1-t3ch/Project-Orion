# Test Index

- `tests/test_reborn_analysis.py`: verifies the static trainer analyzer accounts for every file and clusters representative cheat features.
- `tests/test_docs_coverage.py`: verifies required threat model, runbook, research, and evidence schema docs exist and are non-trivial.

Run:

```powershell
python -m unittest discover -s tests -p "test_*.py"
```
